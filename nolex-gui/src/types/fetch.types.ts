export interface Clinic {
    id: string;
    name: string;
}

export interface BodyPart {
    id: string;
    name: string;
}

export interface Exame {
    id: string;
    name: string;
    min_cod: string;
    internal_code: string;
}
